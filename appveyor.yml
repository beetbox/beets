version: "{build}"
build: off
deploy: off

environment:
    matrix:
        - PYTHON: "C:\\Python27"
          TOXENV: py27

# Install Tox for running tests.
install:
    - "%PYTHON%/Scripts/pip.exe install tox"

test_script:
    - "%PYTHON%/Scripts/tox.exe"
